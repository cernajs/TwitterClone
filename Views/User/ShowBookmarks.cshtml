@model List<Tweet>

@if(Model.Count() == 0)
{
	<div class="alert alert-info">
        You have no Bookamrks!
    </div>
}

<ul id="tweetFeed">
    @foreach(var tweet in Model)
    {
        <li>
            <partial name="_TweetPartial" model="@tweet" />
        </li>
    }
</ul>