@model IList<Tweet>

@if(Model.Count == 0)
{
    <div class="alert alert-info">
        <p>There are no tweets to display.</p>
    </div>
}

<ul>
@foreach(var tweet in Model)
{
    @* <partial name="_TweetPartial" model="@tweet" /> *@
    <li>@tweet.TweetContent</li>
}
</ul>